<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="bike.Views.Settings.ConfigurationPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="http://prismlibrary.com"
    Title="Configurations"
    prism:ViewModelLocator.AutowireViewModel="True"
    BackgroundColor="{DynamicResource Gray-White}">
    <ContentPage.Content>
        <TableView BackgroundColor="Transparent" Intent="Settings">
            <TableView.Margin>
                <OnPlatform x:TypeArguments="Thickness" Android="16" />
            </TableView.Margin>
            <TableSection Title="Core">
                <TextCell Detail="Unique Id" Text="{Binding Core.UniqueId}" />
                <TextCell Detail="Firmware Version" Text="{Binding Core.FirmwareVersion}" />
                <TextCell Detail="Bike Model" Text="{Binding Core.ModelVersion}" />
            </TableSection>
            <TableSection Title="Battery">
                <TextCell Text="{Binding Battery.OverCurrent}" />
                <TextCell Detail="Over Voltage" Text="{Binding Battery.OverVoltage}" />
                <TextCell Detail="Under Voltage" Text="{Binding Battery.UnedrVoltage}" />
                <TextCell Detail="Nominal Voltage" Text="{Binding Battery.NominalVoltage}" />
                <TextCell Detail="Over Temprature" Text="{Binding Battery.OverTemprature}" />
            </TableSection>
            <TableSection Title="Throttle">
                <TextCell Detail="Min Voltage" Text="{Binding Throttle.Min}" />
                <TextCell Detail="Max Voltage" Text="{Binding Throttle.Max}" />
                <TextCell Detail="Fault Voltage" Text="{Binding Throttle.FaultThreshold}" />
            </TableSection>
            <TableSection Title="General">
                <TextCell Detail="Magnet Count" Text="{Binding Pedal.MagnetCount}" />
                <TextCell Detail="Wheel Radius" Text="{Binding Servo.WheelRadius}" />
            </TableSection>
        </TableView>
    </ContentPage.Content>
</ContentPage>